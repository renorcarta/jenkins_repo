pipeline {
    agent any

    environment {
        GITHUB_TOKEN = credentials('LifeTimeServiceAccountToken')
    }

    stages {
        stage('List GitHub Repos') {
            steps {
                sh '''
                    curl -H "Authorization: token $GITHUB_TOKEN" https://github.com/renorcarta/jenkins_repo
                '''
            }
        }
    }
}
